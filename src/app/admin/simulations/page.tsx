
'use client';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminSimulationsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Simulations Library</CardTitle>
        <CardDescription>Create, manage, and view results from phishing, fraud, and other security simulations.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Simulation management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
