'use client';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminAnalyticsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Risk &amp; Analytics</CardTitle>
        <CardDescription>View dashboards and reports on organizational risk.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Analytics and reporting interface will be here.</p>
      </CardContent>
    </Card>
  );
}
