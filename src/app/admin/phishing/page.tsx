'use client';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminPhishingPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Phishing Simulations</CardTitle>
        <CardDescription>Create and manage phishing simulations.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Phishing simulation management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
