'use client';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminMonitoringPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Logs &amp; Monitoring</CardTitle>
        <CardDescription>View system logs and monitor activity.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Monitoring interface will be here.</p>
      </CardContent>
    </Card>
  );
}
