'use client';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminFraudPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Fraud Simulations</CardTitle>
        <CardDescription>Create and manage fraud simulations.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Fraud simulation management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
