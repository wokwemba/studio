'use client';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminVulnerabilitiesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Vulnerability & Patch Management</CardTitle>
        <CardDescription>View and manage discovered vulnerabilities across your organization.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Vulnerability management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
